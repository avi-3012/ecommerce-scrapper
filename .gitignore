# dependencies
node_modules/

# builds
dist/
*.tsbuildinfo

# environment / secrets — never commit real values
.env
.env.*
!.env.example

# logs & OS noise
*.log
.DS_Store

# test artifacts
coverage/

# prisma generated client lives in node_modules; nothing extra to ignore
