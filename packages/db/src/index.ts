/**
 * Database access layer. API and worker import the Prisma client and its
 * generated types from here only — never from @prisma/client directly —
 * so the client's home stays a private detail of this package.
 */
export * from '@prisma/client';
export { PrismaClient } from '@prisma/client';
